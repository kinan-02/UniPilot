# Synthetic Technion-shaped samples

Small **non-production** JSON files that mimic the shape of real Technion semester exports for unit tests and normalizer development.

- `technion_course_list_synthetic.json` — two fake courses with `general` + `schedule` blocks (clearly marked synthetic).

These files are safe to commit. They are **not** real Technion data.
