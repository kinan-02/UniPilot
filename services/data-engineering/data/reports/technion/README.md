# Technion staging quality reports (Phase 10)

Generated by:

```bash
python -m app.main validate-dds-staging-quality
```

- `dds_staging_quality_report.json` — gitignored when large (full machine-readable report)
- `dds_staging_quality_report.md` — human-readable summary (may be committed after review)

Reports are read-only validation output. They do not modify staging or production data.
